Roles

role(x)
role(o)

Estado inicial

init(1,1,b)
init(1,2,b)
init(1,3,b)
init(2,1,b)
init(2,2,b)
init(2,3,b)
init(3,1,b)
init(3,2,b)
init(3,3,b)

init(control(x))

Acciones

(<= (legal ?w (mark ?x ?y))
(true (cell ?x ?y b))
(true (control ?w)))

(<= (legal x noop)
(true (control o)))

<= (legal o noop)
(true (control x)))
